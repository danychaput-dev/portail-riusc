name: Tests CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Permet de declencher manuellement (utile pour E2E)

# Annule automatiquement les runs precedents sur la meme branche quand un nouveau push arrive.
# Evite les faux negatifs ("E2E a plante") declenches par un push intermediaire pendant
# qu'une serie de commits est en cours.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Variables factices pour le build et les tests
  # (les vrais secrets sont dans Vercel pour la prod)
  NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
  NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.placeholder
  NEXT_PUBLIC_N8N_BASE_URL: https://n8n.aqbrs.ca
  NEXT_PUBLIC_MAPBOX_TOKEN: pk.placeholder
  NEXT_PUBLIC_TWILIO_PHONE: "+10000000000"

jobs:
  # ── Tests unitaires (Vitest) ──────────────────────────────────
  unit-tests:
    name: Tests unitaires
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Installer les dependances
        run: npm ci

      - name: Lancer les tests unitaires
        run: npm test

  # ── Tests E2E (Playwright) ───────────────────────────────────
  e2e-tests:
    name: Tests E2E
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Installer les dependances
        run: npm ci

      - name: Installer Playwright
        run: npx playwright install --with-deps chromium

      - name: Lancer les tests E2E
        run: npm run test:e2e
        env:
          CI: true
          E2E_BASE_URL: https://portail.riusc.ca
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          E2E_ADMIN_EMAIL: ${{ secrets.E2E_ADMIN_EMAIL }}

      - name: Sauvegarder les screenshots en cas d'echec
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: test-results/
          retention-days: 7

  # ── Build verification ───────────────────────────────────────
  build-check:
    name: Verification build
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Installer les dependances
        run: npm ci

      - name: Build Next.js
        run: npm run build
        env:
          SUPABASE_SERVICE_ROLE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.placeholder
          ANTHROPIC_API_KEY: sk-ant-placeholder
          RESEND_API_KEY: re_placeholder
